platform:
 - x64
   
cache:
 - node_modules
 - '%APPDATA%\npm-cache'
 - '%USERPROFILE%\.electron'

init:
 - git config --global core.autocrlt input
 - set

install:
 - ps: Install-Product Node
 - npm install

build_script:
 - npm run dist

test: off