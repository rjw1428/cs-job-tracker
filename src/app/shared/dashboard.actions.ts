import { createAction, props } from '@ngrx/store';

export const bidAdded = createAction(
    "[Dashboard Component] Bid Created"
)

export const requeryComplete = createAction(
    "[Dashboard Component] Data Requeried"
)